{
  "name": "stampdag-backend",
  "version": "0.2.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "e2e:install": "playwright install --with-deps",
    "wallet:generate": "node scripts/generate-anchor-wallet.js",
    "lint:syntax": "node --check server.js && node --check db.js && node --check db-pg.js && node --check kaspa-anchor.js && node --check kaspa-crypto.js && node --check certificate.js"
  },
  "dependencies": {
    "@kluster/kaspa-wasm": "^2.0.1",
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.3",
    "pdfkit": "^0.15.0",
    "pg": "^8.20.0",
    "pino": "^9.4.0",
    "pino-http": "^10.3.0",
    "qrcode": "^1.5.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "pino-pretty": "^13.1.3",
    "supertest": "^7.2.2",
    "vitest": "^2.1.0"
  }
}
